name: .NET Core CI

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '9.0.100-preview.5'

      - name: Install dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Publish
        run: dotnet publish -c Release -o ./publish

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Build and Push to ECR
        run: |
          docker build -t coreapiapp .
          docker tag coreapiapp:latest 289193905928.dkr.ecr.ap-southeast-2.amazonaws.com/my_api_app/coreapiapp:latest
          docker push 289193905928.dkr.ecr.ap-southeast-2.amazonaws.com/my_api_app/coreapiapp:latest
